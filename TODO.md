ideas:

- add pre-filled dietpi.txt template with my preferred options, then add a small script that just tweaks it to add the sensitive info (ssh keys, account password, etc), maybe also bakes using `dietpi-baker` directly
